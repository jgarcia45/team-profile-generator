const Employee = require('../lib/Employee');

// Tests for Name
test('gets employee name', () => {

});

// Test for ID
test('gets employee id', () => {

});

// Test for Email
test('gets employee email', () => {
});

// Test for getName()
test('returns name from getName() ', () => {
});

// Test for getId()
test('returns ID from getId()', () => {
});

// Test for getEmail()
test('returns email from getEmail()', () => {
});

// Test for getRole()
test('returns role from getRole()', () => {
});